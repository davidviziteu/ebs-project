syntax = "proto3";

message Subscription {
	message FieldSubscription {
        string key = 1;
        string operator = 2;
        string value = 3;
    }

    repeated FieldSubscription fieldSubscriptions = 1;
}